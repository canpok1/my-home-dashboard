{
  "name": "fetcher",
  "version": "1.0.0",
  "description": "",
  "main": "App.ts",
  "scripts": {
    "build": "tsc",
    "start": "dotenv -e .env ts-node src/App.ts | pino-pretty",
    "clean": "rm -rf output && rm -rf tmp && rm -f tsconfig.tsbuildinfo",
    "lint": "echo \"Warn: no lint specified\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Warn: no test specified\"",
    "install:chromium": "npx -y playwright install --with-deps chromium"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.4.5",
    "@types/node-cron": "^3.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@playwright/test": "^1.37.0",
    "@prisma/client": "^5.1.1",
    "node-cron": "^3.0.2",
    "pino": "^8.15.1",
    "pino-pretty": "^10.2.0"
  }
}
