{
  "name": "fetcher",
  "version": "1.0.0",
  "description": "",
  "main": "App.ts",
  "scripts": {
    "build": "tsc",
    "start": "dotenv -e .env ts-node src/App.ts | bunyan",
    "clean": "rm -rf output && rm -rf tmp && rm -f tsconfig.tsbuildinfo",
    "lint": "echo \"Warn: no lint specified\"",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "install:chromium": "npx -y playwright install --with-deps chromium"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bunyan": "^1.8.10",
    "@types/node": "^20.8.10",
    "@types/node-cron": "^3.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@playwright/test": "^1.37.0",
    "@prisma/client": "^5.6.0",
    "bunyan": "^1.8.15",
    "bunyan-slack": "^1.0.0",
    "node-cron": "^3.0.2"
  }
}
